var ast = 3;

for (var i = 0; i < 1; i++) {
  var coluna = "";
  // AQUI ELE CRIA UMA LINHA EM BRANCO
  for (var j = 0; j < ast; j++) {
    var linha = "*";
    coluna += linha
    console.log(coluna)
  // AQUI ELE INSERE O TANTO DE CARACTERES DO $AST NA LINHA VAZIA
  // CONSOLE LOG DENTRO DO FOR PRA DAR UMA PIRAMIDE
  }
}
